package interview

// TODO:
// 在一个数组中，找到一个最长的升序子数组的长度
func LongestAscendingSubArray(nums []int) int {
	if len(nums) == 0 {
		return 0
	}

	return 0
}

// 在一个数组中，找到最长的升序子数组的个数

// dp[i]：i之前（包括i）最长递增子序列的长度为dp[i]
// count[i]：以nums[i]为结尾的字符串，最长递增子序列的个数为count[i]

func LongestAscendingSubArrayCount(nums []int) int {
	if len(nums) == 0 {
		return 0
	}

	return 0
}
